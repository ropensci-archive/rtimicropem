
language: r

matrix:
- os: linux
  r: oldrel
- os: linux
  r: release
- os: linux
  r: devel
- os: osx
  r: oldrel
- os: osx
  r: release
- os: osx
  r: devel

sudo: false
cache: packages

r_github_packages:
  - jimhester/covr

notifications:
    email:
      on_success: change
      on_failure: change

after_success:
  - Rscript -e 'library(covr); codecov()'

env:
- LINTR_COMMENT_BOT=false
