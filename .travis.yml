language: r
sudo: false
cache: packages

r_github_packages:
  - ramnathv/rCharts
  - jimhester/covr
  - hadley/dplyr
  - hadley/lubridate

notifications:
    email:
      on_success: change
      on_failure: change

after_success:
  - Rscript -e 'library(covr); codecov()'

env:
- LINTR_COMMENT_BOT=false
