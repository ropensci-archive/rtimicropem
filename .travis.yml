language: r
sudo: true

r_github_packages:
  - ramnathv/rCharts
  - jimhester/covr
  - jimhester/lintr
  - hadley/dplyr
  - hadley/lubridate

notifications:
    email:
      on_success: change
      on_failure: change

after_success:
  - Rscript -e 'library(covr); codecov()'

env:
- LINTR_COMMENT_BOT=false
